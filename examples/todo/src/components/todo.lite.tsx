import { useState, onMount } from '@jsx-lite/core';

type TodoProps = {};

export default function Todo(props: TodoProps) {
  const state = useState({
    // Hello world
  });

  onMount(() => {
    // Hello world
  });

  return <div class="Hi">Hello asdfasdf</div>;
}
