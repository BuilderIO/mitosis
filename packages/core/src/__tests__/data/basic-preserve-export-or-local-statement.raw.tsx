const b = 3;
const foo = () => {};

export const a = 3;
export const bar = () => {};
export function run<T>(value: T) {}

type Types = {
  s: any[];
};

interface IPost {
  len: number;
}

export default function MyBasicComponent(props: { id: string }) {
  return <div></div>;
}
