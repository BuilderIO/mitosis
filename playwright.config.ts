import { configFor } from '@builder.io/e2e-app-spec/shared.config';

// This Playwright config is *not* used to run tests, but only for the playwright
// install.

export default configFor('UNUSED', 7501);
